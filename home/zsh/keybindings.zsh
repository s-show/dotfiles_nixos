# keybindings.zsh - Key bindings configuration

# Emacs keybindings
bindkey -e

# zsh-abbr keybindings (require -M emacs option because EDITOR=vim)
# bindkey -M emacs ";" abbr-expand-and-insert
# bindkey -M emacs "Enter" abbr-expand-and-accept

# Custom keybindings
# bindkey -s '^X^I' 'editprompt -e nvim_ime\n'  # Ctrl+X Ctrl+I: Launch nvim_ime
